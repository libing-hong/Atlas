import test from "node:test"; import assert from "node:assert/strict"; import { orchestrateRecommendations } from "./orchestrator"; import { normalizeStudentProfile } from "../student-profile"; import type { ProgrammeDiscoveryProvider } from "./programme-discovery";
const provider:ProgrammeDiscoveryProvider={async discover(_profile,terms){const relation=terms.find(x=>x.term==="Creative Industries Management")?.relation??"highly_related";return[{institution:"KEDGE Business School",programme:"MSc Arts & Creative Industries Management",officialUrl:"https://student.kedge.edu/programmes/arts-creative-industries-management",fieldRelation:relation,discoveryQuery:"official regression search",discoveredAt:new Date().toISOString()}]}};
const profile=normalizeStudentProfile({name:"Regression User",educationHistory:[{id:"durham",country:"英国",institutionNameZh:null,institutionNameEn:"Durham University",degreeLevel:"本科",degreeName:"Bachelor's degree",major:"History of Art",graduationYear:2026,graduationMonth:6,graduationStatus:"graduated",arithmeticAverage:null,weightedAverage:null,officialAverage:null,gpa:null,gradingSystem:"UK honours",prerequisiteCourses:[]}],targetCountries:["法国"],targetSubjects:["艺术管理"],targetIntake:{year:2027,term:"fall"},languageTests:[],maxAnnualTuition:null,tuitionCurrency:"EUR",crossDisciplinePreference:"related_only"});
test("Durham to France arts management discovers KEDGE and keeps 2027 unknown",async()=>{const result=await orchestrateRecommendations({profile,plannedApplicationCount:1,discoveryProvider:provider});const kedge=result.candidates.find(x=>x.institution.includes("KEDGE")||x.officialUrl.includes("kedge.edu"));assert.ok(kedge);assert.notEqual(kedge.recommendationBand,"currently_not_suitable");assert.equal(kedge.timelineStatus,"pending");assert.ok(result.expansions.some(x=>x.term==="Creative Industries Management"));assert.equal(result.profile.educationCountry,"英国")});

