import { ComingSoonPage } from "@/components/ComingSoonPage";

export default function ProfilePlaceholder() {
  return (
    <ComingSoonPage
      title="Profile"
      description="Profile editing is paused during Phase 0.5 so visual validation can focus on the journey operating flow."
    />
  );
}
